format = """
[](fg:#d4c5b9)\
$os\
$username\
[](fg:#d4c5b9 bg:#bdae9d)\
$directory\
[](fg:#bdae9d bg:#8b7355)\
$git_branch\
$git_status\
[](fg:#8b7355)\
$fill\
$c\
$direnv\
$elixir\
$elm\
$golang\
$gradle\
$haskell\
$java\
$julia\
$nodejs\
$nim\
$python\
$rust\
$scala\
$docker_context\
[](fg:#7a8450)\
$time\
$cmd_duration
󱞪
"""

command_timeout = 1000

# Disable the blank line at the start of the prompt
# add_newline = true

# You can also replace your username with a neat symbol like   or disable this
# and use the os module below
[username]
show_always = true
style_user = "bg:#d4c5b9 fg:#2a211c"
style_root = "bg:#c5656b fg:bold black"
format = '[$user ]($style)'
disabled = false

# An alternative to the username module which displays a symbol that
# represents the current operating system
[os]
style = "bg:#9A348E"
disabled = true # Disabled by default

[directory]
style = "bg:#bdae9d fg:#2a211c"
format = "[  $path ]($style)"
truncation_length = 10
truncation_symbol = "…/"
read_only = '🔒'

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
# Keep in mind that the order matters. For example:
# "Important Documents" = " 󰈙 "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important 󰈙 " = " 󰈙 "

[git_branch]
symbol = ""
style = "bg:#8b7355 fg:#f5f5dc"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "bg:#8b7355 fg:#f5f5dc"
format = '[$all_status$ahead_behind ]($style)'

[fill]
symbol = " "

[time]
disabled = false
time_format = "%R" # Hour:Minute Format
style = "fg:#2a211c bg:#7a8450"
format = '[ $time ]($style)'

[cmd_duration]
style = "fg:#bdae9d bg:#000000"
format = "[took $duration]($style) "

[c]
symbol = " "
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[direnv]
disabled = false

[docker_context]
symbol = " "
style = "bg:#06969A"
format = '[ $symbol $context ]($style) $path'

[elixir]
symbol = " "
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[golang]
symbol = " "
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[gradle]
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[nim]
symbol = "󰆥 "
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[python]
symbol = " "
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'

[scala]
symbol = " "
style = "bg:#689f63"
format = '[ $symbol ($version) ]($style)'
